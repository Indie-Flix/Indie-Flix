const videoController = require('../server/controllers/VideoController')

describe('videoController function tests', () => {

  // describe('#createVideo', () => {
  //   it('creates video in the database', () => {
  //     const req = {
  //       body: {
            // createVideo tries to decode the jwt which is passed in on req.cookies. Unsure how to simulate this?
  //       }
  //     }
  //     videoController.createVideo()
  //   })
  // })
})